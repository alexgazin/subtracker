// Test renderer removed: this repository no longer uses mock data for static builds.
// Static generation now only renders public marketing pages (index, pricing, privacy, terms, support, login, register).
// If you need to test server-side templates locally, run the app with a real local database and visit the routes.

console.log('test-render.js is deprecated. Use the real server (npm run dev) with a local database to test dynamic pages.');
process.exit(0);
